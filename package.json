{
  "name": "BLADE_Alpha",
  "version": "0.2.0",
  "private": true,
  "description": "Offline staffing scheduler",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:win": "tauri build --bundles nsis"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
